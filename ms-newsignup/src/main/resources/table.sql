/*CREATE TABLE user(
    id UUID,
    created TIMESTAMP,
    lastLogin TIMESTAMP,
    token VARCHAR(256),
    isActive BOOLEAN,
    email VARCHAR(100),
    name VARCHAR(100),
    password VARCHAR(12),
    PRIMARY KEY (id)
);
CREATE TABLE phone(
    id BIGINT,
    number BIGINT,
    citycode INTEGER,
    countrycode VARCHAR(100)
)*/